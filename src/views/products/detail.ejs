<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../includes/head')  %>
</head>
<body>

    <%- include('../includes/header')  %>

    <main>
        <section class="product-description">
            <article>
                <section>
                    <img src="/products/<%= producto.imagen %>" alt="Cafe de colombia">
                </section>
            </article>
            <article>
                <section class="detail">
                    <h3 class="name-detail"><%= producto.nombre %></h3>
                    <p class="price price-detail">$ <%= producto.precio %> </p>
                    <h4><%= producto.descripcion %></h4>
                    <p><span>Variedad:</span> <%= producto.variedad %> </p>
                    <p><span>Aspeco olfativo:</span>  <%= producto.olfato %> </p>
                    <p><span><%= producto.origen %> </span></p>
                    <p class="highlight"><%= producto.resena %></p>
                    <p class="highlight"> Envíos a todo el país.</p>
                    <p class="highlight price">GRATIS EN COMPRAS MAYORES A $3.500.</p>
                    <form action="/" class="form-description">
                        <fieldset class="weight-form">
                            <label>Elegí el peso</label>
                            <select name="peso" id="peso">
                                <option value="0">Elige una opción</option>
                                <% producto.peso.forEach(element => { %>
                                    <option value="<%= element %>"><%= element %> </option>
                                <% }) %>  
                            </select>
                        </fieldset>
                        <fieldset class="quantity-form">
                            <label>Cantidad</label>
                            <section class="quantity">
                                <button type="button"><i class="fas fa-minus"></i></button>
                                <input type="number">
                                <button type="button"><i class="fas fa-plus"></i></button>
                            </section>
                        </fieldset>
                        <button type="submit" class="buy-button buy-button-d">Añadir al carrito</button>
                    </form>
                    <section class="edit-delete">
                        <nav>
                            <a href="/products/<%= producto.id %>/edit" class="buy-button">Editar</a>
                        </nav>
                    <form action="/products/:id?m=DELETE" method="POST">
                        <input type="hidden" name="id" value="<%= producto.id %>">
                        <button type="submit" class="buy-button">Eliminar</button>
                    </form>
                    </section>
                </section>
            </article>
        </section>
        <section class="product-info">
            <article class="descripcion-producto">
                <h3 class="description-title">Descripción</h3>
                <h4 class="subtitle">Información detallada - <%= producto.nombre %> </h4>
                <p class="p-description"><%= producto.informacion %> </p>
                <h4 class="subtitle">Packaging</h4>
                <p class="p-description"><%= producto.packaging %> </p>
            </article>
        </section>
    </main>
    
    <%- include('../includes/footer')  %>
</body>
</html>